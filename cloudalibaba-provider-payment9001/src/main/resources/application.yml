server:
  port: 9001 #服务端口​

spring:
  application:
    name: nacos-payment-provider #服务名
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848


management:   #暴露要监控的东西
  endpoints:
    web:
      exposure:
        include: "*"
