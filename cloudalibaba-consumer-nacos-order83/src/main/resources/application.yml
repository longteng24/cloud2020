server:
  port: 83 #服务端口​

spring:
  application:
    name: nacos-order-consumer #服务名
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

#消费者要访问的微服务名称
service-url:
  nacos-user-service: http://nacos-payment-provider
