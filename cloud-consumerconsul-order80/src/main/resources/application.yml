server:
  port: 80 #服务端口​

spring:
  application:
    name: cloud-consumer-order #服务名
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
