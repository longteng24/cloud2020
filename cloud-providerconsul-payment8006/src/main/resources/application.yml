server:
  port: 8006 #服务端口​

spring:
  application:
    name: cloud-payment-service #服务名
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
